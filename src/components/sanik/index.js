import React from 'react';

import sanikImage from '../../assets/sanik.png';
import * as Styled from './styles';

const Sanik = (size) => <Styled.Sanik src={sanikImage} size={size} />;

export default Sanik;
